source 'https://rubygems.org'
#
# We use jekyll directly instead of the 'github-pages' gem 
# to bypass the strict Ruby version checks.
gem "jekyll", "~> 3.9.0"

# Using the default theme, so including this:
gem "minima"

gem "csv", "~> 3.3"
gem "bigdecimal", "~> 4.1"
gem "faraday-retry", "~> 2.4"
gem "liquid", "~> 4.0.4"
gem "webrick", "~> 1.9"

# Add any other plugins you use below (e.g., jekyll-feed, jekyll-seo-tag)
group :jekyll_plugins do
  gem "jekyll-feed"
end

gem "base64", "~> 0.3.0"

gem "kramdown-parser-gfm", "~> 1.1"
